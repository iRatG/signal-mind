# Wayfinder Map: MADPAC

Status: charted
Tracker: local markdown
Created: 2026-07-28
Owner: Airat + Logos
Project Codename: MADPAC

## Destination

Reach a handoff-ready OpenSpec for MADPAC: a reliable personal analytical loop that connects news pressure, public speech, market traces, experiment logs, and agentic research process into one practical project.

This map ends when the route from current spike to implementation spec is clear: product contract, signal model, quality gate, report shape, delivery policy, evaluation method, and repository boundary are decided.

## Notes

- Domain: `signal-mind`, branch `news-pressure-radar-spike`.
- Project name clarified on 2026-07-28: call the whole project `MADPAC`. Existing terms remain descriptive subdomains: `News Pressure Radar` for news-pressure collection/reporting and `News-Market Coupling` for market-trace research.
- Current system: 5 active sources, SQLite store, daily/weekly/monthly/history regimen, graph clustering, DeepSeek labeling, `pressure_score_v2`, Markdown reports, JSON run status, systemd timer.
- Current operating stance: analytical contour first, no public product or dashboard yet.
- Product direction clarified on 2026-07-28: the first MVP is for understanding the world through digital traces, not for trading recommendations. Market data is evidence of social/economic reaction, not a buy/sell target.
- Market universe clarified on 2026-07-28: start with Moscow Exchange indices, sector indices, and ruble FX pairs against USD, EUR, and CNY. Individual stocks are out of the pilot unless a later ticket proves they are needed.
- Meta-goal clarified on 2026-07-28: the project is also a training ground for a disciplined multi-agent process. The desired process is: clarify a foggy idea, write a technical/spec artifact, implement a spike, independently review quality, decide whether to accept or iterate, then preserve learnings.
- Delivery is downstream from analysis. Telegram publication remains disabled until explicitly approved.
- Use these skills in later sessions: `wayfinder`, `grilling`/`grill-me` (one question at a time — no `grill-up` skill exists in `mattpocock/skills`, confirmed against the upstream repo tree on 2026-07-31), `cultural-research-story` only if the work touches media/source research, and OpenSpec/spec-driven workflow once the map has enough decisions.
- Work one decision ticket at a time. Research tickets may run in parallel; grilling tickets require Airat in the loop.
- Known current status on 2026-07-28 UTC: daily run completed but `send_allowed=false` due to 1 quality flag; weekly has 7 quality flags; history has 1 quality flag.
- Standing preferences clarified 2026-07-31:
  - The coupling idea originally left both causal directions open — news pressure shaping market traces, and markets moving first with news tone/cadence (e.g. top-10 headlines/hour) shifting afterward. Resolved 2026-07-31 on [Ticket 10](tickets/10-market-coupling-model.md): v0 runs market-first only (resource asymmetry — deep free MOEX history vs. thin RU news history), news-first/bidirectional deferred to stage 2.
  - Airat weighs functionality and the shape of the implementation approach above code polish at this stage — this is a planning-heavy effort, not a production-hardening one.
  - Working order is idea → plan → tools → implementation, in that order; don't let tooling setup or code get ahead of an unresolved decision ticket.
- **Session resume point (2026-07-31, seventh update — Ticket 09 closed this session; verify current commit with `git log --oneline -5` before trusting anything below as pushed):** Tickets 01, 02, 03, 04, 05, 06, 07, 09, and 10 are closed — see each ticket's `## Working Decision` + `## Grilling Transcript` for the full record, not just the one-line gists in `## Decisions So Far` above.
  - `docs/wayfinder/news-pressure-radar/idea-buffer.md` holds cross-ticket architectural ideas that surfaced mid-grill but don't belong to one ticket. Check it before assuming a principle lives only on the ticket that first mentioned it.
  - **Ticket 07 (Delivery Policy) closed this session** — a dense, deliberately-deepened grill (9 primary questions, two of which were expanded into explicit branching decision trees at Airat's request rather than answered flat). Key outcomes: **three private, Airat-only delivery surfaces** from one canonical report — a short/punchy Telegram channel, a detailed email, and a pull-only local web page (email and the local page share the same detailed rendering; Telegram is a structurally different condensed format); a single daily schedule for both push channels, with real-time/intraday alerting explicitly deferred to v1+; a **3-level, metric-driven delivery-decision function** (`send_allowed` → `signal_cluster_count` → `needs_review_count`) that reuses Tickets 02/03/04's existing computed fields rather than inventing new state, and which surfaced a genuine gap mid-session — Ticket 03's severity-b `needs_review` clusters had no channel to surface through on an all-noise day, now fixed with a dedicated Telegram ping so that case can never go silent; text-only content on every channel at v0 (charts/visual polish explicitly deferred, even though a reusable matplotlib chart pattern already exists elsewhere in the repo for an unrelated report); recipient address stored in a config file for v0 but accessed only through one interface function, so swapping to a DB registry later touches no calling code (a direct application of the already-standing "stable interface, swappable internals" principle); and a new black-box, registry-driven threshold `K` (default `1`) gating the needs-review ping, explicitly flagged for future recalibration from the system's own operational logs — routed as a new forward note on Ticket 11, since it's the first concrete candidate for that ticket's still-unresolved log-based recalibration mechanism (distinct from Ticket 12's one-time historical bootstrap). Domain/VPS hosting was raised and explicitly confirmed to stay private (uptime motive only, not publicity) — so Ticket 05's publish-triggered legal-designation revisit does not fire.
  - **Ticket 09 (Repository Boundary) — closed in a later session (2026-07-31, seventh session).** The bounded choice deferred at the end of the sixth session was re-presented at the start of the seventh; Airat chose to grill Ticket 09 immediately despite its formal block. Outcome: **MADPAC extracts into its own independent repository** — the mystery "repo access/push flow restored" criterion turned out to be Airat's own past uncertainty about relating a large undocumented legacy project (`signal-mind`) to a newly-disciplined one (MADPAC), not a technical git problem. Key architectural unlock, verified by grep before accepting: **git-repository boundaries and data-source coupling are independent axes** — MADPAC's actual code already has zero live references to `signal-mind` internals, so the repo split does not need to wait on Ticket 12's snapshot extraction to be "safe." Sequencing decided for practical reasons anyway: run Ticket 12 first inside this checkout, then one clean history-preserving `git filter-repo` cut-over. Full detail on the ticket itself and in `idea-buffer.md`.
  - **Next session default plan is [Ticket 11 - Agentic Delivery Process](tickets/11-agentic-delivery-process.md)** — nothing else in the graph blocks on it, and it has now accumulated 7 forward notes (parameter-registry hierarchy, diagnostic-agent role, cost/model split, Auditor separation, fix-per-spec principle, champion/challenger shadow-run mechanics from Ticket 06, and the `K`-threshold operational-recalibration mechanism from Ticket 07). **[Ticket 12 - Historical Cluster Calibration Study](tickets/12-historical-cluster-calibration.md) can run in parallel whenever convenient** — no live grilling needed, and it now carries extra weight as the direct trigger for Ticket 09's physical repo extraction, not just a numeric input to Ticket 08. See `## Current Frontier` below for the full reasoning behind this order.

## Decisions So Far

- [Ticket 01 - Product Contract](tickets/01-product-contract.md) — solo/Airat-only, daily from day one, retrospective-only correlation framing (no directional forecasts), success measured by a backtested reference correlation rather than gut feel; report/quality-gate/metrics-core kept as three separate layers (Tickets 01+04 / 03 / 11).
- [Ticket 02 - Signal Ontology](tickets/02-signal-ontology.md) — 6 canonical signal types (Main pressure, Rising impulse, Persistent background, Synchronized story, One-source anomaly, Noise/routine) each grounded in existing code, sharing 4 cross-cutting principles: versioned parameter registry, no empty modules, state transitions over permanent labels, and a reused news-plane×market-plane cross-check.
- [Ticket 03 - Quality Gate Contract](tickets/03-quality-gate-contract.md) — hard blockers (run failed/collector errors/no articles/no clusters) stay unconditional; every block, hard or soft, must escalate to Airat, never silent; per-cluster severity replaces all-or-nothing report blocking (severity a = annotate & keep in main signal: `unsupported_number`, `thin_cluster`; severity b = exclude from main signal into a "needs review" section: `single_source_persistent`, `single_source_high_volume`, new `llm_fallback_heuristic`); report-level exclusion-share cap left provisional, spun out to Ticket 12.
- [Ticket 05 - Source Strategy](tickets/05-source-strategy.md) — active MVP set grown from 5 to 14 Russian sources (added RBC via an RSS route that bypasses its Qrator block, gazeta.ru, iz.ru, forbes.ru, banki.ru, novayagazeta.ru, meduza.io + svoboda.org via VPN routing for their Roskomnadzor blocks, agents.media); TASS dropped (explicit RSS-ban in its terms); legal designations (foreign-agent/undesirable-org) treated as descriptive labels, not gates, while the project stays private/non-redistributing — revisit at Ticket 07; RKN network blocks solved by per-source VPN routing, not exclusion; new non-numeric `category_spread` (state-only/independent-only/mixed) report label added; numeric source-diversity thresholds deferred to an expanded Ticket 12; scope stays Russian-only for now.
- [Ticket 10 - Market Coupling Model](tickets/10-market-coupling-model.md) — grilled from a clean slate (explicitly ignoring the pre-existing 2026-07-28 OpenSpec draft as a source of default answers). Market-first for v0 (anomaly in market data → look back at news), not news-first/bidirectional — deferred to stage 2 alongside pairwise index/currency signals; reasoning is a real resource asymmetry (deep, free MOEX history vs. thin RU news history), not a data ceiling — confirmed `ru_news_archive_loader.py` can targeted-backfill arbitrary historical dates for 3-4 of 14 sources today. Instrument universe and event windows (`D-3..D+3`, optional `D+5`) are registry/config-driven, never hardcoded in code — a new project-wide principle extending Ticket 02's parameter registry from thresholds to entities (see `idea-buffer.md`). Anomaly definition uses a pool of several metrics as a raw vector, not one fixed rule or hard gate. News-side "meaningfulness" stays descriptive (source count, sync flag) at v0, not a numeric cutoff — negative (no-news) cases are kept, not discarded. Surfaced two items routed elsewhere rather than decided here: whether the Auditor is a separate agent from the Runner (→ Ticket 11, with a new role-independence/stable-interface principle), and a news-Editor rendering role for Telegram/email output (→ Ticket 04).
- [Ticket 04 - Report Prototype](tickets/04-report-prototype.md) — canonical Markdown-only report shape decided (Editor/multi-channel rendering explicitly deferred to Ticket 07). A canonical↔human-label dictionary (Ticket 02 types ↔ current Russian labels) becomes the single source of truth for section headers; One-source anomaly and Noise/routine get properly separated per Ticket 02 (anomaly = real section, noise = one-line count only); Ticket 03's "needs review" list stays visible in the product report. New project-wide mechanism: a **Product vs. Technical/Ops report mode split** — an all-noise day (run succeeds, no cluster reaches any of the 5 non-noise signal types) sends a visually distinct Technical/Ops report instead of a product report, which is also where Ticket 01's "Overall progress" section now lives. Evidence links (`article.url`, already populated in code but never rendered before) and an attributed watch-next list were added as new report content. Executive Summary opens with a fact-based lead sentence (never an evaluative adjective) before a bullet-count nav list. Concrete example artifact: `docs/wayfinder/news-pressure-radar/prototypes/04-daily-report-example.md`. Reinforced Ticket 11's diagnostic-agent forward note and added a new project-wide "fix per the documented concept, never an ad-hoc patch" principle to `idea-buffer.md`.
- [Ticket 06 - Evaluation Harness](tickets/06-evaluation-harness.md) — split into Track 1 (statistical эталон backtest) and Track 2 (qualitative rubric review), each with its own cycle. Track 1: a frozen, portable 1-year headline-only snapshot (extracted read-only from `hf_news.db`, never a live cross-machine dependency) feeds an independent sentiment index (LLM-scored per headline, not reused from `pressure_score_v2`); correlation method is pre-registered — Spearman per lag (D-3..D+5) × instrument, Benjamini-Hochberg FDR, block-bootstrap significance — anchored against known economic-calendar events (CB rate decisions, Rosstat, earnings, MOEX) as known-answer validation checkpoints. Track 2: a three-role Generator → Evaluator → Airat-audit loop (never self-checking) with a four-state handoff protocol, weekly bounded-choice audits, and a per-signal-type rubric that reuses Tickets 02/03/05's existing criteria plus a new domain-relative historical-baseline check (built by reusing the existing Ouroboros SQL-hypothesis engine, not a new tool). New project-wide mechanisms introduced here: the **Metric Card registry** (every metric self-describes purpose/computation/observed distribution/interpretation bands/window applicability/LLM notes); the **Decision Block** role (self-documenting, history-aware auto-calibration — 3 distinct attempts required, never 3 repeats of the same fix — before mandatory escalation), unifying classification-mismatch calibration, quality-gate diagnosis, and champion/challenger promotion into one mechanism; and a **champion/challenger shadow-deployment split** with Ticket 11 (target metrics here, shadow-run mechanics there). Routed a MOEX-as-news-source proposal to Ticket 05 and a RAG-over-public-disclosures idea to `idea-buffer.md` rather than deciding either here.
- [Ticket 07 - Delivery Policy](tickets/07-delivery-policy.md) — three private, Airat-only delivery surfaces from one canonical report: a short/punchy Telegram channel, a detailed email, and a pull-only local web page (email and the local page share one detailed rendering; Telegram is structurally condensed, not just shorter). Single daily schedule drives both push channels; real-time/intraday alerting explicitly deferred to v1+. Delivery/notification behavior is a **3-level, metric-driven decision function** (`send_allowed` → `signal_cluster_count` → `needs_review_count`), reusing Tickets 02/03/04's existing computed fields rather than new state — surfaced and fixed a real gap where Ticket 03's severity-b `needs_review` clusters had no channel to appear through on an all-noise day. Content stays text-only everywhere at v0 (charts/visual polish deferred, despite a reusable chart-generation pattern existing elsewhere in the repo for an unrelated report). Recipient address lives in a config file for v0, accessed only through one interface function — swapping to a DB registry later touches no calling code. A new registry-driven threshold `K` (default `1`, provisional) gates the needs-review ping, explicitly routed to Ticket 11 for future recalibration from the system's own operational logs. Domain/VPS hosting confirmed to stay private (uptime motive only) — Ticket 05's publish-triggered legal-designation revisit does not fire.
- [Ticket 09 - Repository Boundary](tickets/09-repository-boundary.md) — grilled despite the formal Ticket 08 block, at Airat's explicit request. **Decision: MADPAC extracts into its own independent repository**, not a permanent merge into `signal-mind` — driven by both a data-coupling argument (Airat) and a documentation-discipline/governance-mixing argument (Claude). The mystery "repo access/push flow restored" criterion is retired — traced to its origin and confirmed to have been Airat's own unresolved uncertainty about relating an undocumented legacy project to a newly-disciplined one, not a technical git problem. Key unlock: **git-repository boundaries and data-source coupling are independent axes** — verified by grep that MADPAC's actual code (`analytics/news_pressure_cluster.py`, `scripts/news_pressure_regimen.py`) already has zero live references to `signal-mind` internals, so the repo split doesn't need to wait for Ticket 12's snapshot extraction to be "safe." Sequencing decided anyway for practical reasons (fewer moving parts at once): run Ticket 12 first inside this checkout, then do one clean `git filter-repo` cut-over (history-preserving) moving docs + code + the materialized snapshot together. OpenSpec (Ticket 08) and proven operational stability are both downgraded from pre-extraction gates to parallel/post-move work items.

## Current Frontier

Priority order refreshed 2026-07-31 after Ticket 09 closed:

1. [Ticket 11 - Agentic Delivery Process](tickets/11-agentic-delivery-process.md) — **resume here next session.** Meta-process ticket; nothing else in the graph blocks on it. Has accumulated 7 forward notes now (parameter-registry hierarchy, diagnostic-agent role reinforced across Tickets 03/04, cost/model split, Auditor separation, fix-per-spec-not-ad-hoc-patch, champion/challenger shadow-run mechanics from Ticket 06, and the `K`-threshold operational-log recalibration mechanism from Ticket 07, which needs its cadence/approval mechanics designed here).
2. [Ticket 12 - Historical Cluster Calibration Study](tickets/12-historical-cluster-calibration.md) — runs whenever, in parallel with Ticket 11; no live grilling needed, but its priority is now higher than before: it's no longer just a numeric input to Ticket 08, it's the **direct execution trigger for Ticket 09's already-decided repository extraction** (run this first, inside the current checkout, then do the `git filter-repo` cut-over). Scope covers both the English `hf_news.db` archive and the new 14-source RU set; can likely share its extraction script with Ticket 06's 1-year snapshot (same source, same read-only `ATTACH` mechanism, confirmed reachable this session at `c:\project\signal_mind\db\hf_news.db`).

## Blocked Tickets

- [Ticket 08 - OpenSpec Bridge](tickets/08-openspec-bridge.md) depends on Product Contract, Signal Ontology, Quality Gate Contract, Report Prototype, Delivery Policy (all five now closed 2026-07-31), and Ticket 12 - Historical Cluster Calibration Study (numeric sign-off only — still open; Tickets 02/03/04/05/07/10 themselves are not blocked by it).

## Not Yet Specified

- Whether the system should later become a multi-user product, a private intelligence assistant, or a publishing workflow.
- Whether to add a dashboard, and what exact dashboard decisions become necessary after reports prove useful.
- Whether to use embeddings, lemmatization, or both as the next semantic clustering layer.
- Whether the final project should integrate with Obsidian, Telegram channel publishing, email reports, or all of them.
- Whether agent-written interpretations should be treated as prose summaries only or as structured claims that must pass stricter evidence checks.
- Whether the market coupling layer should use event-study statistics, simple association tables, supervised models, or all of them in stages.
- Which parts of the work should be handled by separate agents versus one main agent with explicit review phases.

## Out Of Scope

- Trading signals or investment advice. This project measures agenda pressure, not buy/sell decisions.
- Individual stock-picking in the first pilot. Sector and broad indices are the first market lens.
- Public automatic publishing before a delivery policy and quality gate are closed.
- Full article scraping as a first-class dependency. The current MVP works from headlines; full text can return only if a later decision proves it necessary.
- A polished SaaS dashboard. Dashboard work starts only after the report contract is useful for repeated use.
- Rewriting the existing spike from scratch. The map should preserve the working regimen and clarify the next decisions around it.

## Suggested Session Order

1. ~~Resolve [Ticket 01 - Product Contract](tickets/01-product-contract.md) with `grill-me`.~~ Closed 2026-07-31.
2. ~~Resolve [Ticket 02 - Signal Ontology](tickets/02-signal-ontology.md) with domain modeling.~~ Closed 2026-07-31.
3. ~~Resolve [Ticket 03 - Quality Gate Contract](tickets/03-quality-gate-contract.md) using current run failures as examples.~~ Closed 2026-07-31.
3a. (parallel, no live grilling required) Run [Ticket 12 - Historical Cluster Calibration Study](tickets/12-historical-cluster-calibration.md) once `db/hf_news.db` is confirmed reachable. Feed results back into Tickets 02, 03, 05, 10 as short revision addenda once ready — does not block step 4 or 5 from closing today.
4. ~~Resolve [Ticket 05 - Source Strategy](tickets/05-source-strategy.md) with research.~~ Closed 2026-07-31.
5. ~~Resolve [Ticket 10 - Market Coupling Model](tickets/10-market-coupling-model.md) before any market-code implementation.~~ Closed 2026-07-31 (grilled from a clean slate; market-first for v0).
6. ~~Build [Ticket 04 - Report Prototype](tickets/04-report-prototype.md) as a concrete markdown prototype.~~ Closed 2026-07-31 (canonical Markdown-only shape; Product/Technical-Ops report mode split; Editor/multi-channel rendering deferred to Ticket 07).
7. ~~Resolve [Ticket 06 - Evaluation Harness](tickets/06-evaluation-harness.md).~~ Closed 2026-07-31 (Track 1 эталон backtest + Track 2 rubric review, Metric Card registry, Decision Block, champion/challenger split with Ticket 11).
8. ~~Resolve [Ticket 07 - Delivery Policy](tickets/07-delivery-policy.md).~~ Closed 2026-07-31 (three private delivery surfaces; 3-level metric-driven delivery-decision function; text-only v0; recipient config behind a stable interface; `K`-threshold routed to Ticket 11).
9. ~~Resolve [Ticket 09 - Repository Boundary](tickets/09-repository-boundary.md), grilled despite its formal block at Airat's request.~~ Closed 2026-07-31 (MADPAC extracts into its own repo; sequencing decided: Ticket 12 first, then a single history-preserving `git filter-repo` cut-over).
10. **← Next.** Resolve [Ticket 11 - Agentic Delivery Process](tickets/11-agentic-delivery-process.md) so the project teaches the workflow, not only the market idea — nothing else blocks on this, so it can slot in whenever convenient. Now also owns the champion/challenger shadow-run mechanics from Ticket 06 and the `K`-threshold operational-recalibration mechanism from Ticket 07.
11. Run [Ticket 12 - Historical Cluster Calibration Study](tickets/12-historical-cluster-calibration.md) (can happen in parallel with step 10) — its snapshot completion is now the direct trigger for Ticket 09's physical repo extraction.
12. Turn the settled decisions into OpenSpec through [Ticket 08 - OpenSpec Bridge](tickets/08-openspec-bridge.md), then execute Ticket 09's extraction.
