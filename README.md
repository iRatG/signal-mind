# Signal Mind — AI-агент для поиска слабых рыночных сигналов

> Система, которая сама генерирует гипотезы, проверяет их на данных, учится на ошибках  
> и автоматически патчит себя после каждого марафона.  
> **3 416 итераций · 14 сессий · ~$10 · 714M токенов · 0 ручных правок между марафонами.**

---

## 1. Подход

Финансовые рынки реагируют на информацию с **задержкой**. Новость о росте нефти сегодня
может отразиться на финансовом секторе через 90 дней — через цепочку причинно-следственных
связей, которую человек не успевает отследить вручную.

Центральная формула проекта:

```
слабый сигнал → доказательства на данных → бизнес-возможность → проверочное действие
```

Агент перебирает **тысячи комбинаций** источников, временных лагов и рыночных режимов.
Каждую итерацию он пишет SQL, выполняет его на живой DuckDB, оценивает результат
и передаёт знания следующей итерации. После каждого марафона запускается аудитор (Revizor),
который находит системные ошибки и **автоматически** патчит конфиги — следующий марафон
начинается уже с исправлениями.

---

## 2. Архитектура

### Три вложенные петли обучения

```
┌─────────────────────────────────────────────────────────────┐
│  Петля 3: cross-session learning                            │
│  experiments.db → датасет для fine-tuning                   │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Петля 2: Ouroboros — per-session                    │  │
│  │  гипотеза → SQL → данные → оценка → новая гипотеза  │  │
│  │                                                      │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │  Петля 1: SQL self-repair — per-iteration      │  │  │
│  │  │  ошибка → классификация (8 типов) → fix        │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
              ↓ после каждого марафона
┌─────────────────────────────────────────────────────────────┐
│  Revizor: автономный аудит + apply_fixes()                  │
│  → current_regime.json  → convergence_blacklist.json        │
│  → forbidden_patterns.md  → sql_patterns.md                 │
└─────────────────────────────────────────────────────────────┘
```

### Многослойная память агента (загружается при старте сессии)

| Файл | Назначение |
|------|-----------|
| `principles.md` | Аналитическая «конституция» — нарушать нельзя |
| `knowledge.md` | Подтверждённые находки прошлых сессий |
| `forbidden_patterns.md` | SQL-антипаттерны (9 явных + 7 автодетект. Revizor) |
| `sql_patterns.md` | Рабочие шаблоны SQL (few-shot примеры) |
| `convergence_blacklist.json` | Темы-ловушки (агент застревал ≥3 раз) |
| `current_regime.json` | Текущий режим рынка (ставка=21%, USD/RUB=85) |

### Источники данных

| Источник | Содержимое | Объём |
|----------|-----------|-------|
| MOEX | 15 индексов 2016–2026 (IMOEX, MOEXFN, MOEXOG...) | 38 000+ строк |
| ЦБ РФ | Ключевая ставка 2014–2026, USD/RUB 2003–2026 | 5 600+ строк |
| Росстат | Зарплаты, реальный индекс, демография, жильё | 15 503 строки |
| Глобальные рынки | SP500, GOLD, BRENT, DXY, MSCI_INDIA, FTSE_CHINA_50... | 13 инструментов |
| HuggingFace News | financial-news-multisource, 24 источника | **2.52M статей / 9.93 GB** |
| ChromaDB RAG | PDF ЦБ РФ + корпоративная отчётность | **17 492 чанка** |

### Технологический стек

| Слой | Технология |
|------|-----------|
| Числовые данные | **DuckDB** — аналитика в файле, 40 мс на запрос |
| Новости | **SQLite** — 9.93 GB, только чтение |
| Векторный поиск | **ChromaDB** + MiniLM-L12-v2 |
| LLM | **DeepSeek API** (`deepseek-chat`) |
| Язык | Python 3.12 |

### Структура проекта

```
signal_mind/
├── src/agent/
│   ├── agent.py         # главный цикл: LAG_SWEEP, TOPIC_POOL, anti-convergence
│   ├── hypothesis.py    # generate → aliasing check → execute_with_repair → evaluate
│   ├── sql_repair.py    # self-repair loop, 8 типов ошибок DuckDB
│   ├── revizor.py       # пост-марафонный аудит + apply_fixes()
│   ├── telemetry.py     # замер токенов, времени, rag/sql/llm по шагам
│   ├── rag.py           # ChromaDB: regulatory + corp поиск
│   ├── memory.py        # 3-уровневая память (principles, knowledge, journal)
│   ├── watchdog.py      # автоперезапуск при падении, time-limit марафон
│   └── experiments.py   # fine-tuning dataset (SQLite, 27 колонок)
├── src/parsers/         # загрузка данных (MOEX, ЦБ, Росстат, PDF)
├── analytics/           # HTML-отчёт, signal_scan, PROJECT_REPORT.md
├── habr/                # статьи и разборы сигналов (не перемещать!)
└── db/                  # базы данных (не в git, тяжёлые)
```

---

## 3. Выводы: что нашли

Три сигнала с независимой SQL-верификацией (прямой запрос к DuckDB, без LLM):

| Сигнал | r | Лаг | n | Стабильность |
|--------|---|-----|---|-------------|
| USD/RUB → MOEXFN | **+0.758** | 14 дней | 990 | ⚠️ 2024 инверсия (r=−0.47) |
| Brent → MOEXFN | **−0.702** | 90 дней | 799 | ⚠️ 2025 ослабевает (+0.26) |
| MSCI_INDIA → MOEXFN | **+0.625** | 14 дней | 983 | ⚠️ только при USD/RUB ≤ 80 |

**Главная аналитическая находка** — сигналы **режимозависимы и нестационарны**:
один и тот же фактор работает с разным знаком в зависимости от уровня ключевой ставки
и курса рубля. Внутри однородного рыночного режима корреляции устойчивее.

> **Честная оговорка:** все три сигнала найдены и проверены на одних и тех же данных
> 2022–2025 (in-sample). Без hold-out теста на 2024–2025 они остаются гипотезами,
> а не торговыми сигналами. Это P0-приоритет следующей фазы (Phase 10).

---

## 4. Ошибка, которую мы нашли сами

После Marathon 1 confirmed rate был 67.7% — слишком высокий, чтобы быть правдой.
Независимая верификация вскрыла системный баг:

```sql
-- Агент писал вот так (НЕПРАВИЛЬНО):
SELECT imoex_close AS ftse_china_50   -- ← подмена колонки!
FROM v_market_context
-- CORR(IMOEX, MOEXOG) = 0.88 — обе стороны — российские индексы

-- Надо было:
FROM market_data WHERE instrument = 'FTSE_CHINA_50'
-- Реальный результат: r = -0.20 (честный)
```

Агент переименовывал `imoex_close` в `ftse_china_50` и мерял корреляцию двух российских
индексов — конечно, r ≈ 0.88. ~60% «подтверждённых» сигналов были тавтологией.

**Реальный clean confirmed rate: ~17–20%.**

Исправления:
- `_check_aliasing()` в `hypothesis.py` — перехватывает до выполнения SQL
- `FOREIGN_INSTRUMENTS` список в `revizor.py` — аудит после марафона
- Паттерн задокументирован в `forbidden_patterns.md` (раздел «instrument aliasing»)
- Датасет `experiments.db` содержит и правильные, и неправильные примеры — это ценнее, чем датасет только с идеальными ответами.

---

## 5. Кейс: нефть предсказывает банки в минус через 90 дней

Самый нетривиальный сигнал: **обратная** корреляция Brent с MOEXFN, лаг 90 дней.

```sql
WITH t AS (
    SELECT d.close AS brent, s.moexfn_finance
    FROM market_data d
    JOIN v_moex_sectors s
        ON s.trade_date = d.trade_date + INTERVAL 90 DAYS
    WHERE d.instrument = 'BRENT'
      AND d.trade_date BETWEEN '2022-01-01' AND '2025-12-31'
      AND d.close IS NOT NULL AND s.moexfn_finance IS NOT NULL
)
SELECT ROUND(CORR(brent, moexfn_finance), 4), COUNT(*) FROM t;
-- correlation = -0.7021 | n = 799
```

![Brent и MOEXFN 2022–2025](habr/sample/charts/chart1_main.png)

**Почему нефть ↑ = банки ↓ через квартал:**
нефть растёт → рубль укрепляется → инфляция снижается → ЦБ снижает ставку
→ банковская маржа сжимается → MOEXFN падает.

Корреляция по лагам монотонно усиливается — признак структурной зависимости, не шума:

![Корреляция по лагам](habr/sample/charts/chart2_lags.png)

**Важный нюанс:** сигнал нестабилен по годам (2023: r=−0.11, 2024: r=−0.10, 2025: r=+0.26).
При высокой ключевой ставке банки зарабатывают на процентных доходах вне зависимости от нефти.

![Режимный анализ](habr/sample/charts/chart6_regime.png)

Полный разбор с SQL, временными рядами и хронологией событий:
[`habr/sample/brent_moexfn_investigation.md`](habr/sample/brent_moexfn_investigation.md)

---

## 6. Итоги всех марафонов

| Марафон | Дата | Итераций | Время | Стоимость | Главное улучшение |
|---------|------|---------|-------|-----------|------------------|
| Marathon 1 «Ночь тишины» | 2026-04-30 | **1 882** | 9 ч | $5.76 | Базовый Ouroboros + Revizor |
| Marathon 2 | 2026-05-01 | 514 | ~3 ч | ~$1.50 | Telemetry + 3 aliasing паттерна |
| Marathon 3 | 2026-05-02 | 650 | ~3 ч | ~$1.90 | WEAK_R auto-blacklist + EUR/RUB тавтология |
| Marathon 4 | 2026-05-02 | 290 | ~1.5 ч | ~$0.85 | Comparative SQL exemption, permanent blacklist |
| **Итого** | | **3 416** | **~18.5 ч** | **~$10** | |

| Метрика | Значение |
|---------|---------|
| Всего экспериментов (experiments.db) | **3 416** |
| Подтверждено (LLM self-eval) | 2 269 (66%) — **завышено** |
| Clean confirmed (независимая верификация) | ~17–20% |
| SQL repairs | **28** успешных, **0** неисправимых |
| Сигналов в signals.jsonl | **3 444** |
| Токенов суммарно | ~714M |

---

## 7. Дорожная карта

| Фаза | Статус |
|------|--------|
| Phase 1–4: Данные (DuckDB, ChromaDB, HF News 2.52M статей) | ✅ |
| Phase 5: Ouroboros агент (SQL + RAG + новости) | ✅ |
| Phase 5.5: Lag sweep, telemetry, watchdog | ✅ |
| Phase 5.6: Analytics report (HTML + Chart.js) | ✅ |
| Phase 6: Верификация + aliasing bug + чистый скан 618 комбинаций | ✅ |
| Phase 7: Revizor (аудит + apply_fixes, автопатч после марафона) | ✅ |
| Phase 8: Anti-convergence (blacklist + fingerprint + TOPIC_POOL rotation) | ✅ |
| Phase 9: Режимная инъекция в evaluator, WEAK_R, comparative SQL exemption | ✅ |
| **Phase 10: Data split (Discovery 2022–23 / Validation 2024 / Live 2025)** | 🔜 **P0** |
| Phase 11: Fine-tuning DeepSeek на experiments.db (3 416 строк) | 🔜 |
| Phase 12: Режимный классификатор (автоопределение состояния рынка) | 🔜 |

---

## Запуск

```bash
# Установка зависимостей
python -m venv .venv
.venv/Scripts/pip install -r requirements.txt

# Одна сессия (N итераций)
PYTHONIOENCODING=utf-8 .venv/Scripts/python -m src.agent.agent 10

# Марафон с автоперезапуском (секунды)
PYTHONIOENCODING=utf-8 .venv/Scripts/python -m src.agent.watchdog 10800   # 3 часа

# Пост-марафонный аудит
PYTHONIOENCODING=utf-8 .venv/Scripts/python -m src.agent.revizor

# Полный отчёт
cat analytics/PROJECT_REPORT.md
```

---

*Python · DuckDB · ChromaDB · DeepSeek API · 2.52M новостей · MOEX · ЦБ РФ · Росстат*
